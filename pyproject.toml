[project]
name = "django-rocketing"
version = "0.1.0"
description = " A Django + React template to kickstart your new project"
readme = "README.md"
requires-python = ">=3.14"
dependencies = ["django>=6.0"]
